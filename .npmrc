tag-version-prefix=""
; vitest's optional peer dep wants @types/node >=18 while the project pins ^16;
; the mismatch is harmless, so let npm install resolve it without --legacy-peer-deps.
legacy-peer-deps=true